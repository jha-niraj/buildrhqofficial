# Secrets. `.env.production` is uploaded with `wrangler deploy --secrets-file`
# and must never be committed.
.env
.env.*
!.env.example
!.env.production.example

# Build + local state
.wrangler/
.turbo/
dist/
node_modules/
